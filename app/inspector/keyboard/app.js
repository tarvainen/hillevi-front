(function () {
    'use strict';

    /**
     * Main module initialization.
     */
    angular.module('Inspector.Keyboard', [
        'Inspector.Keyboard.Services',
        'Inspector.Keyboard.Summary',
        'Inspector.Keyboard.Load'
    ]);

})();